//apply plugin: 'java'
//apply plugin: 'maven'

plugins {
    id("java")
    id('maven-publish')
}

group = "me.yeonhyuk"

repositories {
    mavenCentral()
}

java {
    withSourcesJar()
}

dependencies {
    testImplementation(platform("org.junit:junit-bom:5.10.0"))
    testImplementation("org.junit.jupiter:junit-jupiter")
}

tasks.test {
    useJUnitPlatform()
}

publishing {
    publications {
        release(MavenPublication) {
            from components.java

            groupId = 'com.github.BranKein'
            artifactId = 'module-id-generator'
            version = '1.0'
        }
    }
    repositories {
        maven {
            url = 'https://jitpack.io'
        }
    }
}

wrapper {
    gradleVersion = "8.5"
    distributionType = Wrapper.DistributionType.ALL
}
